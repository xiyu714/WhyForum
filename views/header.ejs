<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>Blog</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="/stylesheets/tent.min.css" rel="stylesheet">
  <link href="/stylesheets/myStyle.css" rel="stylesheet">
  <link rel="icon" href="/favicon.png">
</head>
<body>

  <header class="masthead">
    <h1>e-f I\O</h1>
  </header>
  <main class="mastcontent">
    <div class="container">
      <div class="grid">
        <div class="grid__column--2">
          <nav class="sidebar">
          <a title="主页" href="/" class="type-italic display-block">home</a>
          <% if (user) { %>
            <a title="发表" href="/post" class="type-italic display-block">post</a>
            <a title="登出" href="/logout" class="type-italic display-block">logout</a>
          <% } else { %>
            <a title="登录" href="/login" class="type-italic display-block">login</a>
            <a title="注册" href="/reg" class="type-italic display-block">register</a>
          <% } %>
          </nav>
        </div>
        <div class="grid__column--8">
          <% if (success) { %>
            <div><%= success %></div>
          <% } %>
          <% if (error) { %>
            <div><%= error %> </div>
          <% } %>
          <h1><%= title %></h1>
          <article>
